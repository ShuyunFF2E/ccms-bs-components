(function(angular) {
    angular
        .module('app', ['ccms.bs.components'])
        .controller('ctrl', function($scope, $timeout, $interval) {

            $scope.value;

        });
})(window.angular);
